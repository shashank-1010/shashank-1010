name: Generate snake animation

on:
  schedule:  # execute every 12 hours
    - cron: "0 */12 * * *"  # Fixed cron syntax (minute hour)
  workflow_dispatch:
  push:
    branches:
      - main

jobs:
  generate:
    permissions:
      contents: write
    runs-on: ubuntu-latest
    timeout-minutes: 5

    steps:
      - name: generate snake.svg
        uses: Platane/snk@v3  # Better to use main action
        with:
          github_user_name: ${{ github.repository_owner }}
          outputs: |
            dist/snake.svg?palette=github-dark
            dist/snake-light.svg?palette=github-light  # Optional: light theme too

      - name: push snake.svg to output branch
        uses: crazy-max/ghaction-github-pages@v4  # Latest version v4
        with:
          target_branch: output
          build_dir: dist
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
